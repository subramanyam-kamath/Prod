name: GitHub Actions QA Org
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: 
  workflow_dispatch: 


env:
   GH_TOKEN: ${{ secrets.GITHUB_TOKEN}}

jobs:   
    validate-deployment-on-QA-org:        
        runs-on: ubuntu-latest
        environment: QA-Branch
        steps:            
            - uses: actions/setup-node@v3
              with:
                node-version: '14'

            - name: 'Checkout source code'
              uses: actions/checkout@v3
              with:
                fetch-depth: 0       
            

            - name: 'Read commit Body'
              env:
                 PR_BODY: ${{ github.event.pull_request.body }}

             
              run: |
                  echo $PR_BODY > ./pr_body.txt
                  node ./parsePR.js              
                  TESTS=$(cat testsToRun.txt)       
                  echo "APEX_TESTS=$TESTS" >> $GITHUB_ENV
                  
            
            - name: 'Install Salesforce CLI'
              run: |
                  wget https://developer.salesforce.com/media/salesforce-cli/sfdx/channels/stable/sfdx-linux-x64.tar.xz
                  mkdir ~/sfdx
                  tar xJf sfdx-linux-x64.tar.xz -C ~/sfdx --strip-components 1
                  echo "$HOME/sfdx/bin" >> $GITHUB_PATH
                  ~/sfdx/bin/sfdx version

           
            - name: 'Installing sfdx git delta'
              run: | 
                  echo y | sfdx plugins:install sfdx-git-delta
                  sfdx plugins 

            
            - name: 'Installing java'
              run: |
                sudo apt-get update
                sudo apt install default-jdk

            
            - name: 'Installing SFDX scanner'
              run: sfdx plugins:install @salesforce/sfdx-scanner

            
            - name: Decrypt the server.key.enc file & store inside assets folder
              run: openssl enc -nosalt -aes-256-cbc -d -in ${{ secrets.ENCRYPTION_KEY_FILE }} -out ${{ secrets.JWT_KEY_FILE }} -base64 -K ${{ secrets.DECRYPTION_KEY }} -iv ${{ secrets.DECRYPTION_IV }}
              

            
            - name: Authenticate Salesforce ORG
              run: |
                sfdx force:auth:jwt:grant --clientid ${{ secrets.HUB_CONSUMER_KEY }} --jwtkeyfile ${{ secrets.JWT_KEY_FILE }} --username ${{ secrets.HUB_USER_NAME }} --setdefaultdevhubusername -a HubOrg

            - name: Get date value and append it to branch name
              run: |
                 echo "RELEASE_DATE=$(date --rfc-3339=date)" >> ${GITHUB_ENV}
            
            - name: create new branch
              run: git checkout -b Release/${{env.RELEASE_DATE}} origin/main	

            - name: publish to github
              run: git push origin Release/${{env.RELEASE_DATE}}

            - name: Get all Accounts
              run: |
                sf data query --query "SELECT Id, Name, Account.Name FROM Contact" >output.csv  --target-org HubOrg
                 sf data query --query "SELECT Id, Name, Account.Name FROM Contact" >files/output2.csv  --target-org HubOrg

            - name: git config set
              run: |
                git config --global user.email "subramanyam.kamath@gmail.com"
                git config --global user.name "subramanyam-kamath"

            - name: Push & Commit file
              run: | 
                 git add -A
                 git commit -m "added new file"

            - name: Push file to remote repository.
              run: |
                git push origin Release/version26
              
            - name: Create Pull request against main branch.
              run: |
                 gh pr create --title "Course is created" --body "Course is created" --base main 
                 

            
